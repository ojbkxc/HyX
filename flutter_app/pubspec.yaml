name: hyx_flutter
description: HyX cross-platform client — Flutter UI on the hyx-core Rust engine.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: '>=3.19.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_rust_bridge: 2.12.0

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true