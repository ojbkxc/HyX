name: hyx_isolates
description: HyX Rust bindings via flutter_rust_bridge
publish_to: "none"
resolution: workspace

version: 0.0.1

environment:
  flutter: ^3.41.0
  sdk: ^3.11.0

dependencies:
  collection: ^1.17.2
  flutter:
    sdk: flutter
  flutter_rust_bridge: ^2.12.0
  logging: ^1.3.0
  meta: ^1.17.0
  refena_flutter: ^3.5.0
  rust_lib_hyx_app:
    path: rust_builder

dev_dependencies:
  build_runner: ^2.15.1
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter