# rust_lib_hyx_app

Flutter plugin crate for HyX's Rust core, built by cargokit.
This package wraps the native library produced from `../rust/` and exposes
it to `flutter_rust_bridge` generated bindings in `hyx_isolates/lib/rust/`.