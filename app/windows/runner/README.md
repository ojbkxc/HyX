# HyX — Windows runner placeholder
#
# The actual runner sources (main.cpp, flutter_window.cpp, utils.cpp, resource.h,
# flutter_window.h, utils.h) are generated by `flutter create .` and live in this
# directory. CI is expected to run `flutter create --platforms=windows .` to
# materialize them before building. This placeholder keeps the directory in VCS.