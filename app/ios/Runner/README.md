# HyX — iOS Runner placeholder
#
# The actual Xcode project (Runner.xcodeproj, Runner.xcworkspace, AppDelegate.swift,
# Runner-Bridging-Header.h, Info.plist, Assets.xcassets, etc.) is generated by
# `flutter create .` and lives in this directory and sibling directories.
# CI materializes them before building. This placeholder keeps the directory in VCS.