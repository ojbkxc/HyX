# HyX — macOS Runner placeholder
#
# The actual Xcode project (Runner.xcodeproj, Runner.xcworkspace, MainFlutterWindow.swift,
# AppDelegate.swift, DebugProfile.xcconfig, Release.xcconfig, Assets.xcassets, etc.)
# is generated by `flutter create .` and lives in this directory and sibling
# directories. CI materializes them before building. This placeholder keeps the
# directory in VCS.