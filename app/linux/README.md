# HyX — Linux GTK application placeholder
#
# The actual sources (main.cc, my_application.cc, my_application.h) are generated
# by `flutter create .` and live in this directory. CI materializes them before
# building. This placeholder keeps the directory in VCS.